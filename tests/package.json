{
  "name": "admission-stats-tests",
  "version": "1.0.0",
  "description": "Unit + E2E tests for Leo's Admissions App",
  "scripts": {
    "test": "jest --forceExit --detectOpenHandles",
    "test:unit": "jest unit/ --forceExit",
    "test:e2e": "jest e2e/ --forceExit --detectOpenHandles",
    "seed": "node fixtures/seed.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
