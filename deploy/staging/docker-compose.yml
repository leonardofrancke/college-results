version: '3.8'
services:
  leo-app-staging:
    build:
      context: ../..
    container_name: leo-app-staging
    restart: unless-stopped
    ports:
      - "58293:80"
    volumes:
      - /var/local/docker/leo-app/html:/app/html:ro
      - /var/local/docker/leo-app/api/server.js:/app/api/server.js
      - /var/local/docker/leo-app/staging/db:/app/db
    environment:
      - ADMIN_SECRET=${ADMIN_SECRET}
    networks:
      - webinfra_frontend
    labels:
      com.docker.compose.project: leo-app-staging

networks:
  webinfra_frontend:
    external: true
